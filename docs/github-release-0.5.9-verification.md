# 0.5.9 GitHub 发布修复与验证

## 原因与修复

- 失败任务：`35960531816`，五个平台均在严格 Clippy 检查失败。
- `resolve_vulcan_process_which_in_directory` 接收的目录已经是引用，两个调用再次借用触发 `needless_borrow`，并被 `-D warnings` 拦截。
- 修复提交：`71d94be113b309004c4d4aa2b18ce43b1e867aaf`。删除两处多余借用，不关闭告警，不改变目录解析语义。
- 之前仅确认 crates.io 上传和三平台专项并发测试，遗漏主流水线及 GitHub 资产发布。两者不能相互替代。

## 发布身份

- 发布标签：`v0.5.9`，指向上述完整提交。
- 发布记录编号：`395423718`。
- GitHub 发布构建：[35968213587](https://github.com/LuaSkills/luaskills/actions/runs/35968213587)，五个平台全部成功。
- 完整原生验收：[35968159990](https://github.com/LuaSkills/luaskills/actions/runs/35968159990)，五个平台全部成功，Python 与 TypeScript 示例检查成功。
- crates.io 的 0.5.9 保持不可变；GitHub 标签额外包含两处无行为变化的借用修正及发布说明。未覆盖注册表包，未移动已经发布的标签。

## 下载与运行验证

- 五个平台各四类包：动态库开发包、外部调用示例、直接集成示例、调试工具。
- 20 个压缩包和 20 个校验文件均实际下载，全部 SHA-256 一致。
- 五个平台的动态库架构分别验证为对应的 Windows、Linux、macOS 与处理器目标。
- 公开头文件与源码一致；Windows 检出换行由 LF 转为 CRLF，按换行归一化后的精确文本比对，不把换行转换误判为接口变化。
- Windows 使用下载的动态库和下载的示例执行真实系统租约：创建、调用子进程、读取输出、状态查询、关闭及关闭后拒绝执行均正常。
- Windows 下载的调试工具可以启动并显示帮助。
- 本地严格 Clippy、格式检查、两项进程路径测试、十项系统租约测试通过。

## 注意事项

全量测试逐平台结果如下；每个平台均额外通过 22 项其他目标测试。忽略项均为既有专用环境或性能用例，未改变标记。

| 平台 | 库测试通过 | 既有忽略 | 失败 |
| --- | ---: | ---: | ---: |
| Windows x64 | 706 | 3 | 0 |
| Linux x64 | 676 | 1 | 0 |
| Linux ARM64 | 676 | 1 | 0 |
| macOS x64 | 677 | 1 | 0 |
| macOS ARM64 | 677 | 1 | 0 |

- 现有发布工作流会在上传第一平台时将草稿变为公开发布；不能把草稿状态视为全部平台验收完成的门槛。本次以完整工作流成功、40 项资产齐全及下载校验作为最终发布完成条件。
- 本次没有发布独立的 TypeScript、Python、Go SDK 仓库，也没有修改独立运行时包仓库的版本。
- Windows 全量库测试 706 项通过，其他目标测试 22 项通过；三个专用环境或性能用例保留原有忽略标记，没有扩大忽略范围。
- 非 Windows 平台有原生 CI 执行证据；本机下载后执行的动态库验证只覆盖 Windows。
